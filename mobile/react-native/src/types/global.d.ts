/**
 * Global type declarations for React Native environment.
 *
 * Declares RN-specific globals that TypeScript doesn't know about
 * out of the box. These are provided at runtime by the Metro bundler.
 */

/** True when running in development mode (Metro bundler sets this) */
declare const __DEV__: boolean;
