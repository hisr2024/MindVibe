name: "CI — tests"
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          check-latest: true

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements-dev.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi

      - name: Generate EdDSA keyset
        run: |
          python scripts/generate_eddsa_key.py --dir keyset_eddsa

      - name: Set EDDSA_KEYSET_DIR for tests
        run: |
          echo "EDDSA_KEYSET_DIR=${{ github.workspace }}/keyset_eddsa" >> $GITHUB_ENV

      - name: Run tests
        env:
          EDDSA_ENABLED: ${{ secrets.EDDSA_ENABLED || 'true' }}
          EDDSA_DUAL_SIGN: ${{ secrets.EDDSA_DUAL_SIGN || 'true' }}
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'dev-jwt-secret-please-change' }}
        run: |
          pytest -q# (paste the exact YAML file contents shown above here)
