{
  "name": "MindVibe Devcontainer (Python + Node)",
  "image": "mcr.microsoft.com/devcontainers/python:0-3.11",
  "forwardPorts": [3000, 8000],
  "postCreateCommand": "python -m pip install --upgrade pip && if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi && if [ -f package.json ]; then npm ci; fi && pip install --upgrade git+https://github.com/get-aider/aider.git || true",
  "customizations": {
    "vscode": {
      "extensions": [
        "get-aider.aider",
        "ms-python.python",
        "esbenp.prettier-vscode",
        "eamodio.gitlens"
      ],
      "settings": {
        "editor.formatOnSave": true
      }
    }
  }
}
